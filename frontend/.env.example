# mock uses deterministic local adapters; real uses the authenticated Frappe RPC boundary.
VITE_API_MODE=mock
# Leave empty for same-origin deployment. This value must never contain secrets.
VITE_API_BASE_URL=
