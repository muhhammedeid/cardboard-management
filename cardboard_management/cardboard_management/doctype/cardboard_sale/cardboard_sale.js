frappe.ui.form.on("Cardboard Sale", {
	refresh(frm) {
		// The native Stock Entry and its stock effect stay out of the operator's
		// reach; the administrative section is collapsed by DocType metadata.
	},
});
